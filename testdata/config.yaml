apiVersion: v1
kind: Config
preferences: {}

clusters:
  - cluster:
      server: https://127.0.0.1
    name: mapstore

contexts:
  - context:
      cluster: mapstore
      user: mapstore
    name: mapstore
current-context: mapstore

users:
  - name: mapstore
